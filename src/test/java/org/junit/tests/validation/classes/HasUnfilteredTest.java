package org.junit.tests.validation.classes;

import org.junit.Test;

public class HasUnfilteredTest {
    @Test
    public void unfilteredTest() throws Exception {
        /*
         * unfiltered test to ensure filter-tests don't fail because no
         * tests to execute remain
         */
    }
}
